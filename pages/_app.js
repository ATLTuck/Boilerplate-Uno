import '../styles/globals.css';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

// Expansion Point: Add global state management, themes, or layout components

export default MyApp; 